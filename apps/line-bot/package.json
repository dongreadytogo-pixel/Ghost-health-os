{
  "name": "@ghost/line-bot",
  "version": "0.1.0",
  "private": true,
  "description": "LINE assistant for Ghost Health OS — a Cloudflare Worker that answers Thai health questions via the free-tier Gemini API.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@ghost/application": "workspace:*",
    "@ghost/domain": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
