{
  "name": "@ghost/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Pure, provider-agnostic domain core for Ghost Health OS. No I/O, no framework, fully testable.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
